pull_request_rules:
  - name: Comment when circleci passes
    conditions:
      - files!=.circleci/config.yml
      - "status-success=ci/circleci: build"
    actions:
      comment:
        message: "OK"

