pull_request_rules:
  - name: Comment when circleci passes
    conditions:
      - -files~=\.github/.*
      - -files~=util/.*
      - status-success=Main / tests (pull_request)
    actions:
      comment:
        message: "OK"

