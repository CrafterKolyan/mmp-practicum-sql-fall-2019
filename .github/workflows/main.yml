name: Main

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Getting current directory
      run: pwd
    - name: Run main check
      run: tests/main.py
